# 后端地址（见 docs/10-App打包前检查清单.md）
#
# H5 调试端：**留空即可** —— 相对路径会走 vite.config.ts 的代理，行为与接入前一致。
# App 成品端：**必须填绝对地址**。真机连不上 127.0.0.1（那是手机自己），
#            要填跑后端的这台机器的局域网 IP，例如 http://192.168.1.10:8000。
#
# 用法：复制成 .env.local 再改（.env.local 已被 .gitignore 忽略）；
#      也可以在构建时注入：VITE_API_BASE=http://192.168.1.10:8000 npm run build:app
#
# VITE_API_BASE=http://192.168.1.10:8000
